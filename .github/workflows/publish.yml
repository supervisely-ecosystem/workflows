name: Publish app to production
run-name: Publish app to production
on: workflow_dispatch
jobs:
  Supervisely-Release:
    uses: supervisely-ecosystem/workflows/.github/workflows/common.yml@runner-updates
    with:
      SLUG: "${{ github.repository }}"
      RELEASE_VERSION: ""
      RELEASE_DESCRIPTION: ""
      RELEASE_TYPE: "publish"
      SUBAPP_PATHS: "__ROOT_APP__, subapp"
      MODELS_PATH: ""
      FRAMEWORK: ""
