name: Build App Image From Template
run-name: Build App Image for ${{ github.repository }}

on:
  workflow_call:
    inputs:
      tag_version:
        description: "Docker Image Tag (without 'v')"
        required: false
        type: string
        default: ""
      dockerfile_template_path:
        description: "Path to Dockerfile template in this repo"
        required: false
        type: string
        default: "docker/hardened/Dockerfile.tmpl"
      image_name:
        description: "Docker Image Name (optional)"
        required: false
        type: string
        default: ""
      requirements_file:
        description: "Requirements file path in caller repo"
        required: false
        type: string
        default: "dev_requirements.txt"
      latest_tag:
        required: false
        type: boolean
        default: false
      # cve_checks:
      #   description: "Run CVE checks before pushing image"
      #   required: false
      #   type: boolean
      #   default: false
    secrets:
      DOCKERHUB_USERNAME:
        required: false
      DOCKERHUB_TOKEN:
        required: false

permissions:
  contents: read

jobs:
  build-and-push:
    runs-on: gha-runner-supervisely-ecosystem
    steps:
      - uses: actions/checkout@v6

      - uses: actions/checkout@v6
        with:
          repository: supervisely-ecosystem/workflows
          ref: per-app-dockerimages
          path: workflow

      - name: Info
        run: |
          IMAGE_NAME=${{ inputs.image_name }}
          if [ -z "$IMAGE_NAME" ]; then
            IMAGE_NAME=${GITHUB_REPOSITORY#*/}
          fi
          if [[ "$IMAGE_NAME" != supervisely/* ]]; then
            IMAGE_NAME="supervisely/$IMAGE_NAME"
          fi
          echo "IMAGE_NAME=$IMAGE_NAME" >> "$GITHUB_ENV"
          echo "IMAGE_NAME_SAFE=${IMAGE_NAME#*/}" >> "$GITHUB_ENV"
          echo "Image name: $IMAGE_NAME"
          echo "Tag version: ${{ inputs.tag_version }}"
          echo "Dockerfile template path: ${{ inputs.dockerfile_template_path }}"
          echo "Requirements file: ${{ inputs.requirements_file }}"

      - name: Prepare tag version
        id: prep
        run: |
          TAG_VERSION="${{ inputs.tag_version }}"
          if [ -z "$TAG_VERSION" ]; then
            TAG_VERSION=$(python3 workflow/scripts/resolve_release_tag.py \
              --config config.json \
              --input-release-tag "${{ inputs.tag_version }}")
          fi
          if [ -z "$TAG_VERSION" ]; then
            echo "Tag version is not set. Provide inputs.tag_version or config.json .version." >&2
            exit 1
          fi
          echo "TAG_VERSION=$TAG_VERSION" >> "$GITHUB_ENV"

      - name: Delete .git folder
        run: |
          rm -rf .git

      - name: Prepare Dockerfile path
        run: |
          DOCKERFILE_PATH="workflow/${{ inputs.dockerfile_template_path }}"
          if [ ! -f "$DOCKERFILE_PATH" ]; then
            echo "Dockerfile template not found at $DOCKERFILE_PATH" >&2
            exit 1
          fi
          echo "DOCKERFILE_DIR=$(dirname "$DOCKERFILE_PATH")" >> "$GITHUB_ENV"
          echo "DOCKERFILE_NAME=$(basename "$DOCKERFILE_PATH")" >> "$GITHUB_ENV"

      - name: Build Docker Image ${{ env.IMAGE_NAME }}:${{ env.TAG_VERSION }}
        run: |
          buildctl build \
            --frontend dockerfile.v0 \
            --local context=${{ github.workspace }} \
            --local dockerfile=${{ env.DOCKERFILE_DIR }} \
            --opt filename=${{ env.DOCKERFILE_NAME }} \
            --opt build-arg:REQUIREMENTS_FILE=${{ inputs.requirements_file }} \
            --opt build-arg:tag_ref_name=${{ env.TAG_VERSION }} \
            --output type=image,name=docker.io/${{ env.IMAGE_NAME }}:${{ env.TAG_VERSION }},push=true  \
            --export-cache type=registry,ref=docker.io/${{ env.IMAGE_NAME }}:buildcache,mode=max \
            --import-cache type=registry,ref=docker.io/${{ env.IMAGE_NAME }}:buildcache

      - name: Build Docker Image ${{ env.IMAGE_NAME }}:latest
        if: inputs.latest_tag
        run: |
          buildctl build \
            --frontend dockerfile.v0 \
            --local context=${{ github.workspace }} \
            --local dockerfile=${{ env.DOCKERFILE_DIR }} \
            --opt filename=${{ env.DOCKERFILE_NAME }} \
            --opt build-arg:REQUIREMENTS_FILE=${{ inputs.requirements_file }} \
            --opt build-arg:tag_ref_name=${{ env.TAG_VERSION }} \
            --output type=image,name=docker.io/${{ env.IMAGE_NAME }}:latest,push=true  \
            --import-cache type=registry,ref=docker.io/${{ env.IMAGE_NAME }}:buildcache

      # - name: pip-audit scan
      #   if: ${{ inputs.cve_checks }}
      #   run: |
      #     mkdir -p audit_logs
      #     if ! docker run --rm -v "$PWD/audit_logs:/work" \
      #       "${{ env.IMAGE_NAME }}:${{ env.TAG_VERSION }}" \
      #       sh -lc "/opt/venv/bin/python -m pip_audit --format json > /work/pip_audit.json"; then
      #       echo "pip-audit found vulnerabilities" >&2
      #       exit 1
      #     fi

      # - name: Trivy scan (HIGH/CRITICAL)
      #   if: ${{ inputs.cve_checks }}
      #   run: |
      #     mkdir -p trivy_logs
      #     docker run --rm \
      #       -v /var/run/docker.sock:/var/run/docker.sock \
      #       -v "$PWD/trivy_logs:/work" \
      #       aquasec/trivy:latest image --severity HIGH,CRITICAL --exit-code 1 \
      #       --format json --output /work/${IMAGE_NAME_SAFE}.json \
      #       "${{ env.IMAGE_NAME }}:${{ env.TAG_VERSION }}"

      # - name: Dockle scan (FATAL)
      #   if: ${{ inputs.cve_checks }}
      #   run: |
      #     mkdir -p dockle_logs
      #     docker run --rm \
      #       -v /var/run/docker.sock:/var/run/docker.sock \
      #       -v "$PWD/dockle_logs:/work" \
      #       goodwithtech/dockle:latest --exit-code 1 --exit-level FATAL \
      #       --accept-file '.*settings\.py$' \
      #       --format json --output /work/${IMAGE_NAME_SAFE}.json \
      #       "${{ env.IMAGE_NAME }}:${{ env.TAG_VERSION }}"

      # - name: Generate SBOM (SPDX JSON)
      #   if: ${{ inputs.cve_checks }}
      #   run: |
      #     mkdir -p sbom_logs
      #     docker run --rm \
      #       -v /var/run/docker.sock:/var/run/docker.sock \
      #       -v "$PWD/sbom_logs:/work" \
      #       anchore/syft:latest \
      #       "${{ env.IMAGE_NAME }}:${{ env.TAG_VERSION }}" \
      #       -o spdx-json=/work/${IMAGE_NAME_SAFE}-sbom.json

      # - name: Push image
      #   run: |
      #     if [ -n "${{ secrets.DOCKERHUB_USERNAME }}" ] && [ -n "${{ secrets.DOCKERHUB_TOKEN }}" ]; then
      #       echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin
      #     fi
      #     docker push "${{ env.IMAGE_NAME }}:${{ env.TAG_VERSION }}"

      # - name: Tag and push latest
      #   if: inputs.latest_tag
      #   run: |
      #     docker tag "${{ env.IMAGE_NAME }}:${{ env.TAG_VERSION }}" "${{ env.IMAGE_NAME }}:latest"
      #     docker push "${{ env.IMAGE_NAME }}:latest"

      # - name: Upload Trivy report
      #   uses: actions/upload-artifact@v4
      #   if: ${{ inputs.cve_checks && always() }}
      #   with:
      #     name: trivy-${{ env.IMAGE_NAME_SAFE }}
      #     path: trivy_logs/${{ env.IMAGE_NAME_SAFE }}.json

      # - name: Upload pip-audit report
      #   uses: actions/upload-artifact@v4
      #   if: ${{ inputs.cve_checks && always() }}
      #   with:
      #     name: pip-audit-${{ env.IMAGE_NAME_SAFE }}
      #     path: audit_logs/pip_audit.json

      # - name: Upload Dockle report
      #   uses: actions/upload-artifact@v4
      #   if: ${{ inputs.cve_checks && always() }}
      #   with:
      #     name: dockle-${{ env.IMAGE_NAME_SAFE }}
      #     path: dockle_logs/${{ env.IMAGE_NAME_SAFE }}.json

      # - name: Upload SBOM report
      #   uses: actions/upload-artifact@v4
      #   if: ${{ inputs.cve_checks && always() }}
      #   with:
      #     name: sbom-${{ env.IMAGE_NAME_SAFE }}
      #     path: sbom_logs/${{ env.IMAGE_NAME_SAFE }}-sbom.json
